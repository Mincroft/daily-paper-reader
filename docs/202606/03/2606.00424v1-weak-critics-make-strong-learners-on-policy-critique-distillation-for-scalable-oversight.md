---
title: "Weak Critics Make Strong Learners: On-Policy Critique Distillation for Scalable Oversight"
title_zh: 弱批评者造就强学习者：面向可扩展监督的在策略批评蒸馏
authors: "Can Jin, Jiakang Li, Rui Wu, Eddy Zhang, Dimitris N. Metaxas"
date: 2026-05-29
pdf: "https://arxiv.org/pdf/2606.00424v1"
tags: ["query:opd"]
score: 10.0
evidence: 提出在策略评论蒸馏用于可扩展监督
tldr: 当大型语言模型能力超越人类时，弱监督者难以提供可靠标签，使得可扩展监督面临挑战。本文提出一种弱批评者强监督框架，利用弱模型作为批评者提供非误导的修订方向，并设计渐进式在线批评蒸馏（OPCD）方法，通过过滤高质量批评及自适应自我教师信号将批评指导的行为蒸馏到强模型中。在GSM8K等数学推理和Anthropic HH等对齐基准上，该方法随训练轮次持续提升强模型性能，且批评质量直接影响增益幅度。该工作展示了弱批评者监督的有效性，为实现可扩展的弱到强泛化提供了新路径。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.00424v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 869, \"height\": 982, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.00424v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1719, \"height\": 456, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.00424v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1784, \"height\": 834, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.00424v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1708, \"height\": 725, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.00424v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 775, \"height\": 979, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.00424v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 685, \"height\": 446, \"label\": \"Table\"}]"
motivation: 弱监督者无法提供可靠标签但可能给出非误导的修订方向，利用弱批评者帮助强模型更好运用自身知识以实现可扩展监督。
method: 提出渐进式在线批评蒸馏（OPCD），通过过滤高质量批评并利用自适应自我教师信号将批评引导的行为蒸馏到强模型中。
result: 在推理和对齐基准上，OPCD方法在训练中持续提升强模型性能，批评质量直接影响提升幅度。
conclusion: 弱批评者强监督框架为使用弱监督进行可扩展监督提供了有效路径，有助于实现弱到强泛化。
---

## 摘要
随着大型语言模型变得更强，弱监督者可能无法为复杂输出提供可靠的标签、偏好或最终判断，从而限制了弱到强的泛化和可扩展监督。我们研究了一种更易处理的弱监督形式：使用弱模型作为批评者而不是标注者或评判者。弱批评者无需解决任务或选择正确答案，只需提供非误导性的修订方向，帮助强模型更好地运用自身知识。我们将这种设置称为*弱批评者强监督*。我们首先证明弱批评可以在推理时改进冻结的强模型，并且批评质量是这一改进的关键。随后，我们提出渐进式在策略批评蒸馏（**OPCD**），该方法筛选高质量批评，并通过自适应自教师信号将批评引导的行为蒸馏到强模型中。在推理和对齐基准上的实验表明，我们的方法能在训练轮次中持续改进强模型，为利用弱监督实现可扩展监督提供了一条有效路径。

## Abstract
As large language models become stronger, weak supervisors may fail to provide reliable labels, preferences, or final judgments for complex outputs, limiting both weak-to-strong generalization and scalable oversight. We study a more tractable form of weak supervision: using a weak model as a critic rather than as a labeler or judge. Instead of solving the task or selecting the correct answer, the weak critic only needs to provide a non-misleading revision direction that helps the strong model better use its own knowledge. We call this setting *weak-critic strong oversight*. We first show that weak critiques can improve frozen strong models at inference time, and that critique quality is key to this improvement. We then propose progressive on-policy critique distillation (**OPCD**), which filters high-quality critiques and distills critic-guided behavior into the strong model through adaptive self-teacher signals. Experiments on reasoning and alignment benchmarks show that our method improves strong models over training epochs, suggesting an effective path for scalable oversight with weak supervision.

---

## 论文详细总结（自动生成）

# 中文论文总结：Weak Critics Make Strong Learners: On-Policy Critique Distillation for Scalable Oversight

## 1. 论文的核心问题与整体含义

- **研究动机**：随着大型语言模型能力不断增强，人类或弱模型作为监督者时，难以对复杂输出（如数学推理、代码生成等）提供可靠标签、偏好或最终判断，这限制了弱到强泛化（weak-to-strong generalization）和可扩展监督（scalable oversight）。
- **核心思想**：提出一种更易处理的弱监督形式——**弱批评者强监督**（weak-critic strong oversight）。即弱模型不再充当标注者或判决者，只充当批评者，为强模型提供非误导性的修订方向（如指出推理不完整、缺少边界条件检查等），帮助强模型运用自身已有知识。这种方式降低了对弱监督者的要求，因为批评比直接求解任务或判断答案容易得多。
- **整体含义**：该工作探索了利用弱批评实现可扩展监督的有效路径，实验证明弱批评不仅能提升推理时性能，还能通过蒸馏内化到强模型中，随训练不断改善。

## 2. 论文提出的方法论：核心思想与技术细节

- **核心思想**：弱模型提供批评 → 过滤高质量批评 → 利用批评引导的强模型作为自教师 → 蒸馏到无批评的强模型（学生）中，实现渐进式在策略批评蒸馏（OPCD）。
- **关键技术细节**：
    - **推理时批评-修正范式**：强模型生成初始答案 → 弱模型评论 → 强模型基于质询+初始答案+评论进行修正。证明了弱批评能提升冻结强模型性能，且批评质量至关重要。
    - **批评生成与过滤（Critique Generation & Filtration）**：
        - 每个训练轮次，当前强模型生成若干在策略答案（on-policy answers）。
        - 弱模型对每个答案生成一个评论。
        - 过滤条件：结果正确性检查（修正后的答案是否正确） + 规则检查（评论是否相关有用）。只有同时通过两者的评论才保留。
    - **在策略批评蒸馏（On-Policy Critique Distillation）**：
        - 对于每个保留的三元组 (x, y, f)，教师是固定参数的当前强模型，但提供评论作为条件（π_θ(·|x, f, y_{<t})）；学生是同一强模型但不访问评论（π_θ(·|x, y_{<t})）。
        - 优化目标：最小化沿答案轨迹的 token 级 KL 散度：
          L_OPCD = (1/|S_e|) Σ_{(x,y,f)∈S_e} Σ_{t=1}^{|y|} KL[ π_θ(·|x, y_{<t}) || stopgrad(π_θ(·|x, f, y_{<t})) ]
        - 这样学生模型内化批评引导的行为，测试时无需评论。
    - **渐进式更新**：每个轮次，更新后的强模型生成新的在策略答案，弱模型提供新评论，过滤后构成新训练集。批评和数据随策略动态更新，而非固定离线集。

- **算法流程（文字描述）**：
    1. 初始化强模型 π_θ0、弱批评者 π_w、训练集 X、轮次 E、组大小 G。
    2. 对每个轮次 e=0,...,E-1：
       a. 对每个输入 x∈X，从当前强模型采样 G 个在策略答案。
       b. 弱模型为每个答案生成评论。
       c. 对每个答案进行修正，并应用结果+规则过滤，保留高质量 (x, y, f) 到集合 S_e。
       d. 在 S_e 上最小化 L_OPCD 更新 θ_e → θ_{e+1}。

## 3. 实验设计：数据集、基准与对比方法

- **任务场景**：
    - **推理任务**：GPQA Diamond（非思考模型）、AIME 2024+2025（思考模型）；训练/验证使用 GPQA main 的非Diamond子集（250题，129训练+121验证）。
    - **对齐任务**：IFEval（指令遵循），训练数据来自 Magpie 对齐语料（5000条指令-响应对，2500用于微调弱模型，2500用于 OPCD 训练），验证使用 IFEval 测试集。
- **模型对**：
    - 非思考模型：弱模型 Phi-4-mini-instruct (3.8B)，强模型 Phi-4-14B。
    - 思考模型：弱模型 Qwen3-1.7B，强模型 Qwen3-8B（皆启用思考模式）。
    - 训练实验：推理场景用 Qwen3-4B-base（强）+ Qwen3-1.7B-base（弱）；对齐场景用 Phi-4-14B（强）+ Phi-4-mini-instruct（弱，SFT后提供评论）。
- **对比方法**：
    - **推理时实验**：
        - S only：强模型独立多次采样。
        - S + W critic + refine：强模型初始答案 + 弱模型评论 + 修正。
        - 批评质量分析：S + best critic / random critic / worst critic（基于修正后正确性挑选评论）。
        - W only：弱模型独立采样。
    - **训练时实验**：本文主要汇报 OPCD 的训练动态，与初始模型（epoch 0）对比，报告 mean@8 和 pass@8。

## 4. 资源与算力

- 论文**未明确说明**使用的 GPU 型号、数量或总训练时长。只提供了超参数细节：
    - 训练 batch size=128，PPO mini-batch size=32，micro-batch size per GPU=1。
    - 最大 prompt length=4096，最大响应长度对齐8192/推理6000。
    - 优化器 AdamW，学习率对齐1e-6、推理5e-7。
    - 采样温度等参数详见原文。
- 因此：无法从文中获取具体的硬件资源及训练耗时信息。

## 5. 实验数量与充分性

- **实验数量**：
    - 推理时：两个基准（GPQA Diamond, IFEval） + 一组思考模型实验（AIME24+25），并且对批评质量做了三档对比（best/random/worst）。
    - 训练时：两个场景（对齐 IFEval，推理 GPQA 子集），展示了详细的训练动态表格（26步+多轮）和趋势图。
    - 消融：通过批评质量分组间接分析了批评的作用；训练过程中观察了 loss 和分数变化。
- **充分性评估**：
    - **优点**：覆盖了推理和对话两种典型任务，包括非思考型与思考型模型，多种模型对，以及从推理时到训练时的完整验证。批评质量分析有说服力。
    - **不足**：训练实验仅有一个训练/验证集分割（GPQA非Diamond子集用于推理，IFEval用于对齐），没有跨多个独立数据集的验证，也未报告在其他基准（如MATH、GSM8K、MMLU）上的结果。消融仅针对推理时批评质量，训练时缺少对过滤策略、蒸馏目标变体、不同弱模型等的消融实验。
    - **公平性**：对比方法恰当；弱模型先SFT再提供评论，符合弱监督设定。但训练时基线仅为初始强模型自身（无其他弱监督基线如直接蒸馏弱模型答案/logits），可能不公平。不过论文明确强调其设定与弱监督对齐（弱模型不提供完整标签），所以对比重点是自身改善。

## 6. 论文的主要结论与发现

1. **弱批评能在推理时改进冻结的强模型**：在 GPQA Diamond（pass@16: 83.84→90.40）和 IFEval（pass@16: 77.82→90.76）上，加入弱评论修正显著优于多次独立采样。思考模型 Qwen3 上同样有效（AIME pass@16: 86.67→90.00）。
2. **批评质量是改进的关键**：高质量（best）评论带来最大增益，低质量（worst）评论甚至可能损害性能（GPQA pass@16 略低于 Sonly）。
3. **OPCD 在训练中持续提升强模型性能**：对齐任务上 mean@8 从 61.60 提升至 68.74，pass@8 从 75.97 提升至 80.41；推理任务上 mean@8 从 38.22 提升至 42.67，pass@8 从 70.25 提升至 76.86。
4. **推理与对齐呈现不同训练动态**：对齐任务训练稳定（loss下降），推理任务 loss 上升、score 波动大，说明推理任务下弱批评信号噪声更高。

## 7. 优点

- **方法创新**：提出批评式弱监督这一新范式，比直接提供标签/偏好更易获取，更贴近真实人机交互。
- **模型无关性**：批评可以应用于冻结模型（推理时），也可以通过蒸馏内化到模型参数中。
- **渐进式在线蒸馏**：答案、批评、训练集随模型更新动态变化，避免静态离线数据的过时问题。
- **批评过滤机制**：结合结果正确性和规则检查，有效筛选有用批评，避免噪声污染训练。
- **实验设计合理**：从推理时到训练时逐步验证，批评质量消融有说服力，展示了训练动态（loss、score、验证指标），体现了 OOPCD 的迭代提升。

## 8. 不足与局限

- **实验覆盖有限**：
    - 仅在两个任务（推理、对齐）各一个基准上做训练实验，缺乏更多多样化的任务（如代码生成、对话安全、数学多步推理）和更大规模模型（如7B以上）的验证。
    - 推理训练只用了 GPQA 非Diamond子集（250题），规模较小；验证集仅121题，统计显著性可能不足。
- **缺乏关键消融**：
    - 没有消融过滤机制的必要性（只用结果或只规则会怎样）。
    - 没有比较不同蒸馏方式（如仅在正确答案上训练、或使用其他教师信号）。
    - 没有比较直接使用弱模型评论作为标签 vs. 强模型自蒸馏 baseline。
- **批评质量依赖外部检查**：训练时过滤依赖修正后答案的正确性（需要可验证的答案，如多项选择题），在开放式生成或难验证任务中可能不可行。
- **对弱模型要求依然存在**：虽然批评比完整标签容易，但弱模型仍然需要具备一定批评能力，且论文中弱模型经过了 SFT 微调，若弱模型完全不相关可能无效。
- **资源信息缺失**：未报告训练耗时、GPU型号，使得可重复性有一定挑战。
- **偏差风险**：训练集来自 Magpie（合成数据）和 GPQA（领域科学），可能存在数据分布偏差，泛化到真实用户场景需谨慎。

（完）
