---
title: "Bias-Controlled Primal-Dual Natural Actor-Critic: Optimal Rates for Constrained Multi-Objective Average-Reward RL"
title_zh: 偏差控制的原对偶自然演员-评论家：约束多目标平均奖励强化学习的最优速率
authors: "Ankur Naskar, Swetha Ganesh, Vaneet Aggarwal"
date: 2026-06-23
pdf: "https://arxiv.org/pdf/2606.25012v1"
tags: ["query:multi-reward"]
score: 9.0
evidence: 约束多目标平均奖励强化学习
tldr: "针对平均奖励多目标约束强化学习中非线性标量化导致的策略梯度偏差问题，本文提出基于多层蒙特卡洛（MLMC）的原对偶自然演员评论家算法。该算法无需混合时间知识，通过MLMC控制标量化目标、约束评估和演员评论家估计中的偏差，理论证明达到全局最优收敛率和约束违反率的$\\tilde{O}(1/\\sqrt{T})$。这是平均奖励多目标约束强化学习领域首个实现最优收敛且无需混合时间信息的结果。"
source: arxiv
selection_source: fresh_fetch
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.25012v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1185, \"height\": 425, \"label\": \"Table\"}]"
motivation: 平均奖励多目标约束强化学习中非线性标量化引入偏差，现有方法收敛率非最优且依赖混合时间先验知识。
method: 提出基于MLMC的原对偶自然演员评论家算法，在标量化、约束和估计中控制偏差，无需混合时间知识。
result: "算法达到全局最优收敛率和约束违反率的$\\tilde{O}(1/\\sqrt{T})$。"
conclusion: 首个在平均奖励多目标约束强化学习中实现最优收敛且无混合时间依赖的算法。
---

## 摘要
在无限期平均奖励设置下的许多强化学习问题中，需要优化多个相互冲突的目标，同时满足多个安全约束。一种常见的方法是凹标量化，其中智能体最大化效用 $ f(J^π_{r_1}, \ldots, J^π_{r_M}) $，同时满足标量化约束 $ g(J^π_{c_1}, \ldots, J^π_{c_N}) \ge 0 $，其中 $J^π_{r_m}$ 和 $J^π_{c_n}$ 分别表示策略 $π$ 下的平均奖励和代价。然而，$f$ 和 $g$ 的非线性会在策略梯度和演员-评论家方法中引入偏差，因为必须使用 $J^π$ 的有噪声估计来评估梯度，且 $ \mathbb{E}[\partial f(J^π)] \neq \partial f(\mathbb{E}[J^π]),$ 这种偏差会通过原始和对偶更新传播。我们提出了一种基于MLMC的原对偶自然演员-评论家算法，用于平均奖励MDP，该算法在标量化目标、约束评估和演员-评论家估计中控制偏差，无需知道混合时间。我们证明该算法实现了最优的全局收敛和约束违背速率 $ \tilde{O}(1/\sqrt{T}) $。据我们所知，这是首个在平均奖励设置下（无论有无约束）为凹标量化多目标强化学习建立最优收敛的结果，也是首个在即使没有标量化的情况下无需混合时间信息即可实现此结果的工作。

## Abstract
Many reinforcement learning (RL) problems in the infinite-horizon average-reward setting require optimizing multiple conflicting objectives while satisfying multiple safety constraints. A common approach is concave scalarization, where the agent maximizes a utility $ f(J^π_{r_1}, \ldots, J^π_{r_M}) $ subject to a scalarized constraint $ g(J^π_{c_1}, \ldots, J^π_{c_N}) \ge 0 $, where $J^π_{r_m}$ and $J^π_{c_n}$ denote the average-reward and cost under policy $π$. However, the nonlinearity of $f$ and $g$ introduces bias in policy-gradient and actor-critic methods, since gradients must be evaluated using noisy estimates of $J^π,$ and $ \mathbb{E}[\partial f(J^π)] \neq \partial f(\mathbb{E}[J^π]),$ and this bias propagates through both primal and dual updates. We propose an MLMC-based primal-dual Natural Actor-Critic algorithm for average-reward MDPs that controls bias in scalarized objectives, constraint evaluation, and actor-critic estimation without requiring mixing-time knowledge. We show that the algorithm achieves optimal global convergence and constraint-violation rates of $ \tilde{O}(1/\sqrt{T}) $. To our knowledge, this is the first result establishing optimal convergence for concave scalarized multi-objective RL in the average-reward setting, both with and without constraints, and the first to do so without mixing-time information even in the absence of scalarization.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **研究动机**：现实中的强化学习系统（如网络资源分配、推荐系统、自主控制）通常需要在长期平均奖励设置下同时优化多个相互冲突的目标（如效率、公平性、风险），并满足安全约束。凹标量化（concave scalarization）是一种灵活的建模方法，即最大化效用函数 \( f(J^π_{r_1},\dots,J^π_{r_M}) \) 并满足标量化约束 \( g(J^π_{c_1},\dots,J^π_{c_N}) \geq 0 \)。然而，非线性标量化会引入梯度估计偏差：\( \mathbb{E}[\partial f(X)] \neq \partial f(\mathbb{E}[X]) \)，这种偏差在演员-评论家方法和原对偶更新中传播，导致收敛分析困难。
- **核心问题**：现有工作要么处理参数化策略（但无法处理标量化多目标），要么处理凹标量化（但依赖模型/表格方法，无法扩展），或者在折扣奖励设置下处理但无约束。在平均奖励设置下，同时处理参数化策略、约束、凹标量化且不依赖混合时间知识，仍是一个空白。
- **整体含义**：本文首次在平均奖励多目标约束强化学习中，为参数化策略实现了全局最优收敛率 \( \tilde{O}(1/\sqrt{T}) \)，且无需混合时间先验知识。

### 2. 方法论
- **核心思想**：提出基于多层蒙特卡洛（MLMC）的原对偶自然演员-评论家算法（MO-PDNAC），联合控制来自标量化、演员-评论家估计和原对偶更新的耦合偏差，避免混合时间依赖。
- **关键技术细节**：
  - **问题建模**：求解约束优化 \( \max_\theta f(J^θ_r) \text{ s.t. } g(J^θ_c) \geq 0 \)，转化为原对偶鞍点问题 \( \max_{\theta \in \Theta} \min_{\lambda \geq 0} L(\theta,\lambda) = f(J^θ_r) + \lambda g(J^θ_c) \)。
  - **算法流程**（外层循环 K 次，内层循环 H 次）：
    - 采样轨迹，利用 MLMC 估计量（如 \( \hat{q}_u^{\text{MLMC}}, \hat{P}^{\text{MLMC}}, \hat{b}_u^{\text{MLMC}}, \hat{F}^{\text{MLMC}} \)，以及标量化梯度 \( \partial_m f(\hat{J}^θ_r), \partial_n g(\hat{J}^θ_c) \) 和约束代价 \( g(\hat{J}^θ_c) \)）来降低偏差。
    - **评论家估计**：通过线性随机递归 \( \xi_{u}^{h+1} = \xi_u^h + \gamma_\xi(\hat{q}_u^{\text{MLMC}} - \hat{P}^{\text{MLMC}} \xi_u^h) \) 更新特征参数 \( \xi_u = [J_u, \zeta_u]^\top \)。
    - **NPG 估计**：通过 SGD \( w^{h+1} = w^h - \gamma_w \hat{\nabla}_w L_{\nu_{\theta_k}} (\theta_k, \lambda_k, w^h) \) 更新自然策略梯度方向，其中 \( \hat{\nabla}_w L \) 使用 MLMC 估计的 Fisher 矩阵 \( \hat{F} \) 和优势函数 \( \hat{b}_u \)。
    - **参数更新**：\( \theta_{k+1} = \theta_k + \alpha w_k,\; \lambda_{k+1} = \Pi_{[0,2/\delta]}(\lambda_k - \beta g_k) \)。
  - **MLMC 设计**：使用几何分布 Geom(1/2) 采样的轨迹长度 \( \ell = 2^Q, Q \sim \text{Geom}(1/2) \)，结合短轨迹和长轨迹的差值，在保持相同偏差阶数的同时将期望样本数降至 \( O(\log^2 T_{\max}) \)，从而消除混合时间依赖。
- **理论贡献**：在标准假设（遍历性、Slater 条件、光滑性、Fisher 非退化等）下，证明目标误差和约束违反率均达到 \( \tilde{O}(1/\sqrt{T}) \)，且不依赖混合时间。

### 3. 实验设计
- **论文性质**：本文为纯粹的理论工作，**没有进行实验验证**。所有结果均为理论收敛性分析和证明（包括 24 页的数学推导）。
- **对比基准**：理论对比了现有工作（如表 1 所示），包括：
  - 平均奖励演员-评论家（无标量化、无约束）：[14] 达到 \( \tilde{O}(1/\sqrt{T}) \) 但需混合时间。
  - 平均奖励原对偶演员-评论家（单目标、约束）：[33] 需混合时间知识达到最优率，否则次优。
  - 平均奖励基于模型的方法（标量化但无参数化）：[2,1,3] 达到 \( \tilde{O}(1/\sqrt{T}) \)。
  - 折扣奖励策略梯度（标量化、无约束、无参数化）：[13] 样本复杂度 \( O(\epsilon^{-2}) \)。
- **无实际数据集或场景**：理论分析基于一般性的马氏决策过程（MDP），无需指定具体数据集。

### 4. 资源与算力
- **未提及**：文中没有任何关于 GPU 型号、数量、训练时长或计算资源的描述。作为理论论文，不涉及实际训练。

### 5. 实验数量与充分性
- **实验数量**：无实验。论文仅提供理论证明和引理（如 Lemma 1-7, Theorem 1-4），未进行仿真或基准测试。
- **充分性评估**：理论分析在标准假设（Assumption 1-8）下进行，推导完备，但缺乏实验验证。由于该领域此前没有类似理论结果，本文的贡献主要在理论层面，实验缺失不影响其理论价值，但限制了实践可验证性。

### 6. 主要结论与发现
- **全局最优收敛率**：目标函数误差 \( f(J^{π^*}_r) - \frac{1}{K}\sum_{k=0}^{K-1} \mathbb{E}[f(J^{θ_k}_r)] = \tilde{O}(1/\sqrt{T} + \sqrt{\varepsilon_{\text{bias}}} + \sqrt{\varepsilon_{\text{app}}}) \)。
- **约束违反率**：平均约束违反 \( -\frac{1}{K}\sum_{k=0}^{K-1} \mathbb{E}[g(J^{θ_k}_c)] = \tilde{O}(1/\sqrt{T} + \sqrt{\varepsilon_{\text{bias}}} + \sqrt{\varepsilon_{\text{app}}}) \)。
- **无需混合时间**：所有参数（步长、轨迹长度、迭代次数）均不依赖混合时间，这是对现有最优方法 [33] 的改进。
- **首次实现**：在平均奖励设置下，同时处理参数化策略、约束、凹标量化，并达到最优收敛率。

### 7. 优点
- **理论创新性**：首次为平均奖励多目标约束 RL 建立最优收敛率，且无需混合时间知识。
- **偏差控制的系统性**：通过 MLMC 联合控制标量化偏差、演员-评论家估计偏差和原对偶更新偏差，分析清晰。
- **鲁棒性**：算法不需要知道底层 MDP 的混合时间，更贴近实际应用。
- **通用性**：作为副产品，即使在没有标量化的单目标情况下，也改进了 [33] 的结果（无需混合时间即可达到最优率）。

### 8. 不足与局限
- **缺乏实验验证**：论文未提供任何实验（仿真或 benchmark），实际性能未知。理论假设（如遍历性、强制 Fisher 非退化、特征函数逼近误差有界）在真实场景中可能难以满足。
- **假设较强**：需要 Slater 条件（可行解内点）、函数逼近误差 \( \varepsilon_{\text{app}} \) 和参数化误差 \( \varepsilon_{\text{bias}} \) 有界，这些假设限制了应用范围。
- **仅针对平均奖励设置**：未讨论折扣奖励或有限回合设置。
- **未处理状态/动作空间连续**：虽然允许参数化策略，但状态/动作空间假设有限（finite），需要进一步扩展至无限空间。
- **复杂度未详细分析**：虽然给出了样本复杂度阶数，但没有讨论具体常数或实际实现中的效率。

（完）
