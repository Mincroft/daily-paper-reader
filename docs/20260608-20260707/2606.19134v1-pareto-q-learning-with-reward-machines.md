---
title: Pareto Q-Learning with Reward Machines
title_zh: 带有奖励机器的帕累托Q学习
authors: "Arnaud Lequen, Clément Legrand-Lixon, Léo Saulières"
date: 2026-06-17
pdf: "https://arxiv.org/pdf/2606.19134v1"
tags: ["query:multi-reward"]
score: 10.0
evidence: 多目标强化学习结合Pareto Q学习与奖励机
tldr: 多目标强化学习中，非马尔可夫奖励结构导致样本效率低下。本文提出PQLRM，将Pareto Q-Learning与奖励机结合，利用RM分解奖励信号来维护向量化Q估计的Pareto前沿。实验表明，PQLRM收敛速度快于朴素PQL基线，并能合成QRM无法得到的Pareto最优策略，为复杂奖励下的多目标学习提供有效方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.19134v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 818, \"height\": 420, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.19134v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1767, \"height\": 525, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.19134v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 826, \"height\": 982, \"label\": \"Table\"}]"
motivation: 现有单目标QRM无法处理多个冲突目标，而PQL直接应用于交叉乘积MDP样本效率低，需结合RM结构提升多目标学习效率。
method: PQLRM在PQL的向量Q估计中融入RM的状态分解，通过维护每个RM状态的Pareto前沿集合，实现策略的并行优化。
result: 在非马尔可夫奖励测试中，PQLRM收敛速度比PQL基线更快，且能发现QRM无法覆盖的Pareto最优策略。
conclusion: PQLRM有效融合了RM的结构化奖励与PQL的多目标能力，为多目标非马尔可夫任务提供了高效且更优的解决方案。
---

## 摘要
我们提出了带有奖励机器的帕累托Q学习（PQLRM），这是一种多目标强化学习算法，用于奖励结构由一组奖励机器（RM）指定的任务。PQLRM结合了帕累托Q学习（PQL）——它维护向量值Q估计的集合以近似帕累托前沿——以及带有奖励机器的Q学习（QRM）的增强功能，后者利用奖励信号的分解自动机结构。这产生了一种多策略算法，在非马尔可夫的、RM编码的奖励下仍保持样本高效。实验表明，PQLRM比应用于叉积MDP的朴素PQL基线收敛更快，并且可以合成QRM无法生成的帕累托最优策略。

## Abstract
We present Pareto Q-Learning with Reward Machines (PQLRM), a multi-objective reinforcement learning algorithm for tasks whose reward structure is specified by a set of reward machines (RMs). PQLRM combines Pareto Q-Learning (PQL), which maintains sets of vector-valued Q-estimates to approximate the Pareto front, with enhancements from Q-Learning with Reward Machines (QRM), which exploits the factored automaton structure of the reward signal. This yields a multi-policy algorithm that remains sample-efficient under non-Markovian, RM-encoded rewards. Experimental trials show that PQLRM converges faster than a naive PQL baseline applied to the cross-product MDP and can synthesize Pareto-optimal policies that QRM cannot.

---

## 论文详细总结（自动生成）

# 论文中文详细总结

## 1. 核心问题与整体含义（研究动机和背景）
- **背景**：强化学习（RL）中，真实世界任务往往涉及多个冲突目标（如性能、安全、能耗），需要多目标强化学习（MORL）来学习帕累托最优策略集。现有方法中，Pareto Q-Learning（PQL）通过维护向量化Q值集合近似Pareto前沿，但直接应用于复杂奖励结构（如非马尔可夫奖励）时样本效率低。同时，Reward Machines（RMs）能结构化表示非马尔可夫奖励，通过在RM状态间分解价值函数加速学习（QRM）。
- **核心问题**：如何将RM的结构化奖励分解优势与PQL的多策略学习能力相结合，在非马尔可夫多目标任务中实现样本高效、能合成完整Pareto前沿的学习算法。

## 2. 论文提出的方法论：核心思想、关键技术细节
- **核心思想**：将QRM的RM状态分解更新机制融入PQL，在每一个联合RM状态（多个RM的状态组合）上独立维护一组Pareto主导的向量化Q估计，从而利用单个环境转移同时更新所有RM状态的Q集合，大幅提升样本效率。
- **关键技术细节**：
  - 定义**MOMDPRM**：包含环境状态空间、动作空间、奖励机器向量（每个目标一个RM）、标签函数等。
  - **PQLRM算法流程**（Algorithm 1）：
    1. 初始化：对每个环境状态-动作对及每个联合RM状态 `v`，初始化一个空的Q向量集合 `𝑄^{v}_{set}(s,a)`。
    2. 每幕初始重置环境状态 `s` 和当前联合RM状态 `u`。
    3. 交互每步：选择动作（基于当前 `u` 的Q集合），执行得到 `s'` 和标签 `σ`。
    4. **并行更新**：对每个联合RM状态 `v`：
       - 计算 `v'`（根据标签的跃迁），合成向量奖励 `r`（每个目标的即时奖励）。
       - 更新未来非主导向量集合 `ND^{v}(s,a)`（从 `s'` 的所有动作的Q集合中收集非主导向量）。
       - 更新平均即时奖励 `ℛ^{v}(s,a)`。
    5. Q集合重构：`𝑄^{v}_{set}(s,a) = ℛ^{v}(s,a) + γ * ND^{v}(s,a)`。
    6. 更新环境状态和实际RM状态 `u`。
  - **策略重建**：在初始状态选定一个目标Q向量，之后每一步更新残差目标并选择Q集合中向量最接近的动作。

## 3. 实验设计：场景、基准、对比方法
- **环境**：
  - **Pressurized Bountiful Sea Treasure (PBST)**（图1a）：网格世界，三个目标（最小时间/最大宝藏价值/最小压力），压力惩罚非马尔可夫，需RM。
  - **Office World (OW)**（图1b）：办公室导航，多个递送 / 巡逻 / 避障任务组合。
- **基线方法**：
  - **PQL**：将RM状态笛卡尔积入环境状态，形成交叉乘积MDP后应用标准PQL。
  - **QRM**：单目标Q学习，仅在每个任务上独立学习最优策略。
- **指标**：多策略算法（PQL, PQLRM）报告**归一化超体积**；QRM报告**归一化期望奖励和**。
- **评估频率**：每2000步评估一次。

## 4. 资源与算力
- 实验硬件：AMD Ryzen 7 PRO 5850U CPU（未使用GPU），内存上限8 GB，每次运行时间限制10分钟。
- 实现语言：Python 3.10，基于Gymnasium接口。
- **未明确说明**：训练总时长、并行运行数、具体算力消耗（如GPU型号/数量），文中只提到CPU环境，但未给出详细算力统计。

## 5. 实验数量与充分性
- **共4组实验**（图2a-d）：
  - 实验1（图2a）：PBST环境，3个目标。
  - 实验2（图2b）：Office World，3个任务（到办公室、拿咖啡、拿邮件）。
  - 实验3（图2c）：Office World，3个任务（不碰装饰到办公室、拿咖啡到办公室、拿邮件到办公室）。
  - 实验4（图2d）：Office World，2个任务（按顺序巡逻A-B-C-D后到办公室、不碰装饰到办公室）。
- **充分性评估**：
  - **优点**：覆盖了不同复杂度（目标数2-3），包含非马尔可夫压力奖励（实验1）和多种任务组合。
  - **不足**：
    - 未进行消融实验（如移除QRM更新模块、改变RM共享策略等），无法量化各组件贡献。
    - 仅对比了PQL和QRM两个基线，缺少与其它先进MORL算法（如ENVQ、MOPPO等）的比较。
    - 未报告多次运行的标准差或置信区间，结果鲁棒性验证不足。
    - 仅使用确定性MDP，未扩展到随机环境。

## 6. 论文的主要结论与发现
- PQLRM收敛速度比朴素PQL（交叉乘积MDP）更快，尤其在复杂任务（Office World装饰避免、巡逻）中，PQL甚至完全失败（超体积为0），而PQLRM仍能学习。
- PQLRM能合成完整的Pareto最优策略集，而QRM只能学习各单目标最优策略（无法覆盖Pareto前沿上的折中策略）。
- 实验1（PBST）中，两算法均找到15个Pareto最优策略，但PQLRM略快；实验2-3中PQLRM明显快于PQL；实验4中PQL失败，PQLRM成功。

## 7. 优点
- **方法创新**：将RM结构分解与PQL的向量化Pareto维护自然结合，实现多策略基础上的样本效率提升。
- **样本效率**：通过QRM的并行更新机制，单次转移可更新所有联合RM状态的Q集合，避免了交叉乘积MDP的指数级状态空间增长。
- **多策略灵活性**：能比单目标QRM生成更丰富的Pareto前沿，用户可事后选择折中方案。
- **实验多样性**：涵盖非马尔可夫奖励（PBST）、多种任务组合（Office World），验证了算法在不同挑战下的有效性。

## 8. 不足与局限
- **假设限制**：仅适用于确定性MDP（文中明确指出），未扩展到随机环境，实用性受限于任务确定性。
- **计算开销**：联合RM状态空间大小为 `|U_1| * ... * |U_d|`，当目标数量增加或RM状态数增长时，维护的Q集合数量可能爆炸，文中未讨论扩展性。
- **实验覆盖不足**：
  - 缺少消融实验（如分离RM更新与PQL的贡献）。
  - 未与其它主流MORL算法（如基于标量化的多目标策略学习）对比。
  - 未报告统计显著性检验，单次运行结果可能不可靠。
- **算法实现细节**：未讨论epsilon-greedy策略的具体参数、学习率调整等，实验可复现性受限。
- **结论泛化**：仅在两个网格世界任务上测试，真实复杂场景（如机器人控制）尚未验证。

（完）
